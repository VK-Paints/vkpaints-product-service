apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-deploy
  namespace: vkpaints-dev-ns
spec:
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app: product-service
            topologyKey: "kubernetes.io/hostname"
      containers:
      - name: product-container
        image: ghcr.io/vk-paints/vkpaints-product-service:v1.0.0
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
        envFrom:
        - configMapRef:
            name: product-config
        # - secretRef:
        #     name: product-secret
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 10

        ports:
        - containerPort: 3000
